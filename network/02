# Identifiez et affichez les caracteristiques de l'interface Ethernet :

# a) Indiquez l'adresse de brodcast
ip address | grep "inet " | grep "brd" | cut -d' ' -f8

# b) Indiquez toutes les adresses IP qui font partie du meme sous-reseau
/sbin/arp -a

